name: 1pub
version: 0.1.6
description: A HORUS robotics project
author: neos-builder
license: Apache-2.0
language: rust
horus_id: null  # Auto-generated on first dependency resolution
session_id: "1pub-session"  # Shared session for multi-process communication

dependencies:
  - horus
  - horus_library  # Standard robotics messages (CmdVel, etc.)

  # For path dependencies (local development):
  # my_driver:
  #   path: "./drivers/my_driver"

# Optional: Ignore files, directories, and packages during horus run/check
# ignore:
#   files:
#     - "debug_*.py"
#     - "test_*.rs"
#     - "**/experiments/**"
#   directories:
#     - "old/"
#     - "experiments/"
#   packages:
#     - "ipython"
#     - "jupyter"
